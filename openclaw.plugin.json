{
  "id": "agent-cron",
  "name": "Agent cron",
  "description": "Agent-scoped scheduled tasks: list, add, remove, and run cron jobs for this agent only. Always isolated sessions and announce delivery.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
