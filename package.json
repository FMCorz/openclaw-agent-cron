{
  "name": "@openclaw/agent-cron",
  "private": true,
  "description": "Agent-scoped cron tool: list, add, remove, run cron jobs for this agent only.",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
